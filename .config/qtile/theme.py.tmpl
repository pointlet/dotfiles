"""Shared theme values for Qtile configuration.

Generated via themegen/render.py. Edit palette.toml and this template, then rerun the renderer.
"""

# Fonts (smaller to reduce rendered surface size)
BAR_FONT = "$$$FONT$$$"
BAR_FONT_SIZE = $$$FONT_SIZE$$$
BAR_PADDING = $$$PADDING$$$

# Dimensions / spacing
BAR_HEIGHT = $$$BAR_HEIGHT$$$
BORDER_WIDTH = $$$BORDER_WIDTH$$$
OUTER_GAP = $$$OUTER_GAP$$$
INNER_GAP = $$$INNER_GAP$$$

# Colors
COLOR_FOREGROUND = "$$$FOREGROUND$$$"
COLOR_INACTIVE = "$$$MUTED$$$"
COLOR_ACTIVE_BORDER = "$$$BACKGROUND$$$"
COLOR_URGENT = "$$$DANGER$$$"
COLOR_BORDER_FOCUS = "$$$BORDER_FOCUS$$$"
COLOR_BORDER_NORMAL = "$$$BORDER$$$"

# Volume bar colors
COLOR_VOLUME_BAR_ACTIVE = "$$$FOREGROUND$$$"
COLOR_VOLUME_BAR_INACTIVE = "$$$MUTED$$$"
COLOR_VOLUME_BAR_MUTED_ACTIVE = "$$$DANGER$$$"
COLOR_VOLUME_BAR_MUTED_INACTIVE = "$$$DANGER_SOFT$$$"

# Bar 
BAR_BACKGROUND = "$$$TRANSPARENT$$$"
CLOCK_FORMAT = "$$$CLOCK_FORMAT$$$"

# Wallpaper (None avoids decoding/keeping a large image in memory)
WALLPAPER_PATH = "$$$WALLPAPER_PATH$$$"
WALLPAPER_MODE = "$$$WALLPAPER_MODE$$$"
